{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node scripts/railway-clean-next-build.cjs && npm run build && node scripts/railway-verify-next-build.cjs"
  },
  "deploy": {
    "startCommand": "npm run db:migrate:deploy && npm run start:railway",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
