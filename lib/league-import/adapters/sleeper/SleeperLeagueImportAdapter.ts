import { SleeperAdapter } from './SleeperAdapter'

/**
 * Sleeper-specific alias for import adapter wiring used by league creation/import flows.
 * Keeps adapter naming explicit for provider workflows while reusing the canonical adapter.
 */
export const SleeperLeagueImportAdapter = SleeperAdapter
