'use client'

/**
 * Placeholder for post-tribal reveal overlay (spoiler-safe until `tribalRevealAt`).
 * Wire to: SSE `survivor_elimination` / Redis fan-out, `buildEliminationChimmyPrompt`, commissioner reveal mode.
 */
export function SurvivorEliminationOverlayStub() {
  return null
}
