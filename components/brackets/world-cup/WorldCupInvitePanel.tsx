"use client"
import { useState } from "react"
import { Copy, Share2 } from "lucide-react"
import type { WorldCupChallengeView } from "@/lib/world-cup/types"
export default function WorldCupInvitePanel({ view }: { view: WorldCupChallengeView }) { const [copied, setCopied] = useState(false); const inviteUrl = view.challenge.inviteUrl || (typeof window !== "undefined" ? `${window.location.origin}/join/bracket/${view.challenge.inviteCode}` : ""); async function copyInvite() { await navigator.clipboard?.writeText(inviteUrl); setCopied(true); setTimeout(() => setCopied(false), 1400) } async function shareInvite() { if (navigator.share) await navigator.share({ title: view.challenge.name, text: "Join my AllFantasy World Cup bracket challenge", url: inviteUrl }); else await copyInvite() } return <div className="mx-auto max-w-2xl px-4 py-6 pb-28 sm:pb-8"><div className="rounded-lg border border-white/10 bg-white/[0.04] p-5"><h2 className="text-lg font-black text-white">Invite Link</h2><p className="mt-1 text-sm text-white/45">Share this with logged-in users so they can join and make picks.</p><div className="mt-4 break-all rounded-lg border border-white/10 bg-black/40 p-3 text-xs text-white/65">{inviteUrl}</div><div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={copyInvite} className="inline-flex items-center gap-2 rounded-lg bg-cyan-300 px-4 py-2 text-sm font-black text-black"><Copy className="h-4 w-4" />{copied ? "Copied" : "Copy"}</button><button type="button" onClick={shareInvite} className="inline-flex items-center gap-2 rounded-lg border border-white/10 bg-white/[0.04] px-4 py-2 text-sm font-bold text-white/75"><Share2 className="h-4 w-4" />Share</button></div></div></div> }
