"use client"
import { Crown, Medal, RefreshCw } from "lucide-react"
import type { WorldCupChallengeView } from "@/lib/world-cup/types"
export default function WorldCupLeaderboard({ view, onRecalculate, busy }: { view: WorldCupChallengeView; onRecalculate?: () => void; busy?: boolean }) { const current = view.participant?.id; return <div className="mx-auto max-w-4xl px-4 py-5 pb-28 sm:pb-8"><div className="mb-4 flex items-center justify-between gap-3"><div><h2 className="text-lg font-black text-white">Leaderboard</h2><p className="text-xs text-white/45">Last synced {view.challenge.lastSyncedAt ? new Date(view.challenge.lastSyncedAt).toLocaleString() : "not yet"}</p></div>{view.isOwner || view.isAdmin ? <button type="button" onClick={onRecalculate} disabled={busy} className="inline-flex items-center gap-2 rounded-lg border border-white/10 bg-white/[0.04] px-3 py-2 text-xs font-bold text-white/70"><RefreshCw className={`h-3.5 w-3.5 ${busy ? "animate-spin" : ""}`} />Recalculate</button> : null}</div><div className="space-y-2">{view.leaderboard.map((row) => <div key={row.participantId} className={`grid grid-cols-[3rem_1fr_auto] items-center gap-3 rounded-lg border p-3 ${row.participantId === current ? "sticky bottom-20 z-10 border-cyan-300/40 bg-cyan-300/10 sm:static" : "border-white/10 bg-white/[0.03]"}`}><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-black/30 text-sm font-black text-white">{row.rank === 1 ? <Crown className="h-4 w-4 text-amber-200" /> : row.rank <= 3 ? <Medal className="h-4 w-4 text-white/70" /> : row.rank}</div><div className="min-w-0"><div className="truncate text-sm font-black text-white">{row.displayName}</div><div className="truncate text-[11px] text-white/40">Champion: {row.championPickName ?? "none"} · Correct {row.correctPicks}</div></div><div className="text-right"><div className="text-xl font-black text-white">{row.totalScore}</div><div className="text-[10px] text-white/35">max {row.maxPossibleScore}</div></div></div>)}</div></div> }
