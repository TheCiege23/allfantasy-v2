/**
 * Canonical entry re-export for league home (shell + tabs).
 * Primary implementation: `LeagueHomeClient.tsx` (used by legacy flows).
 */
export { default } from '@/components/league/LeagueHomeClient'
