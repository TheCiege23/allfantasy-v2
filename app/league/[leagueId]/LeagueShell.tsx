'use client'

/**
 * League tab shell — placeholder until LEAGUE_PAGE_TASK tabs are implemented.
 * Center content continues to use DashboardShell → LeagueCenterContent for now.
 */
export function LeagueShell() {
  return null
}
